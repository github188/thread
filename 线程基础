(1)线程的四种状态：就绪/运行/阻塞/终止
(2)线程的 分离属性：
    由pthread_create()函数的第二个参数指定。
    分离一个线程并不影响它的运行，只是通知OS，该线程结束时，回收其资源。
    而没有被分离的线程，其终止后，会保留他的资源(虚拟内存、堆栈等)－－僵尸线程
  
(3)线程的退出
  若进程中任意一个线程 调用了：exit() _exit()，那么整个进程就会终止
